if [[ -n "${PRM_FS}" ]]; then
    PRM_ROOT_DIR="/groups/${GROUP}/${PRM_FS}"
fi
if [[ -n "${PRM_FS}" ]]; then
    TMP_ROOT_DIR="/groups/${GROUP}/${TMP_FS}"
fi
if [[ -n "${PRM_FS}" ]]; then
    SCR_ROOT_DIR="/groups/${GROUP}/${SCR_FS}"
fi
HASHDEEP_VERSION='4.4-foss-2015b'
